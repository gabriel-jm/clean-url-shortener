import { describe, it } from 'test-suite'
import { expect } from 'chai'

describe('External', () => {
  describe('Internal()', () => {
    it('should be true', () => {
      expect(true).to.equal(true)
    })
  })
})
