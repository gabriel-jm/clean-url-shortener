{
  "imap": "import-map.json",
  "scripts": {
    "start": {
      "allow": ["net"],
      "cmd": "src/main/index.ts"
    },
    "test": "deno test tests/**/*.spec.ts",
    "test:watch": "deno test --watch tests/*/*/*.spec.ts"
  }
}