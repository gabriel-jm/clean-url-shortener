{
  "imap": "import-map.json",
  "tsconfig": "tsconfig.json",
  "scripts": {
    "start": {
      "allow": ["net"],
      "cmd": "src/main/index.ts"
    },
    "test": "deno test tests/",
    "test:watch": "deno test --location=http://localhost --watch tests/"
  }
}